// import fetchMock from 'jest-fetch-mock'; // Removed due to incompatibility

// fetchMock.enableMocks(); // Removed

// You can also add other global test setup here, like:
// import '@testing-library/jest-dom'; // if you want to extend expect globally
// However, it's often imported directly in test files or a specific test setup file for testing-library.
// For now, this file can be empty or used for other global Vitest setups if needed.

// To ensure the file is not empty and is a valid module if required by any tooling:
export {};
